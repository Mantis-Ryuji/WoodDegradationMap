# AGENTS.md

## 1. Purpose and Scope

このファイルは、特定の言語、フレームワーク、データセット、ディレクトリ構成に依存しない、リポジトリ共通の作業規約を定める。

- ユーザーが依頼した調査、設計支援、実装、修正、レビューを、依頼の目的と完了条件まで進める。初回の実装や提案だけで、許可された残作業を打ち切らない。
- プロダクト方針、研究方針、仕様、優先順位、実験条件、評価方法の最終決定はユーザーが行う。既存の仕様や慣例で判断できる局所的な実装はCodexが進める。
- 変更は目的を満たす最小限とし、既存の責務、互換性、ユーザーの変更を保つ。依頼外の機能追加、最適化、リファクタリング、整形、ファイル整理は提案に留める。
- 調査・説明・レビューのみの依頼はread-onlyとする。変更依頼には、必要なファイルの作成・編集と、その変更に起因する不具合の修正を含む。プログラム実行は第4節に従う。

## 2. Instructions and Context

リポジトリ内の判断は、現在のユーザー指示、対象に最も近い `AGENTS.md`、上位の `AGENTS.md`、関連する設計文書・仕様書・ADR、README等の運用文書、既存コード・テスト・設定の慣例の順に優先する。同順位では、より具体的で新しい情報を優先する。

- 読む資料と調査範囲はタスクに合わせる。構成や入口にはREADME、仕様やデータ契約には該当する設計文書、実行方法には手順書や設定を参照し、関係のない文書やリポジトリ全体の把握を毎回の前提にしない。
- 検索は `rg` と `rg --files` を優先する。必要に応じて `git status`、`git diff`、`git log` で既存変更や履歴を確認する。
- スキルは適用条件がタスクに合うものを使い、補助資料は必要になった箇所だけ読む。スキルの一般的な推奨よりユーザーの明示的な指示を優先する。
- 指示を理由に作業を止める場合は、該当ファイル・規定と、未解決の判断を具体的に示す。

## 3. Decision Boundaries

依頼や会話ですでに決まっている事項は再確認しない。命名、局所的な構成、既存仕様に沿った実装方法など、結果の意味や互換性を変えない判断は自律的に進め、重要な仮定だけ完了時に示す。

次の事項が未決定で、依頼や既存の仕様から解決できない場合は、影響する作業に進む前に確認する。

- 要件の解釈、研究上の仮定、データの意味、評価条件によって、結果や結論が実質的に変わる。
- public API、永続化形式、schema、CLI、設定形式、ディレクトリ構造の互換性を破る必要がある。
- 新しい依存関係・サービス・認証情報・実行環境、または依頼範囲外の変更が必要になる。
- 復旧が難しい削除・上書き・移動・改名が必要、あるいはユーザーの未コミット変更の意図を保って安全に編集できない。
- 文書・テスト・実装の不一致が結果に影響し、指示の優先順位でも解決できない。

確認は理由、影響範囲、選択肢を簡潔に示す。回答を待つ間も、その判断に依存しない許可済みの作業は進める。

## 4. Execution and Git

ファイルの閲覧・検索とGitの状態・差分確認は、個別の許可なく行ってよい。調査目的のコマンドではファイルを変更・生成しない。

プログラム実行は原則としてユーザーが行う。明示的な実行依頼がない限り、Codexは次を実行せず、必要なコマンド、目的、想定される負荷・副作用、確認点を提示する。

- プロジェクトコードやスクリプト、test、lint、format、type check、build、benchmark
- training、evaluation、preprocessing、データ全件走査・hash・重複判定・変換、metadata生成
- data download、crawling、外部サービスへの接続、ネットワークアクセス
- package install、依存関係の更新、環境変更、Docker build、container実行

実行を依頼された場合は、その範囲で結果確認、依頼した変更に起因する不具合の修正、必要な再実行まで進め、各段階の許可を取り直さない。長時間・大規模・GPU・課金・外部状態の変更を伴う処理は、対象と影響が未合意なら実行前に確認する。

Gitの書き込み操作（add、commit、push、pull、branch作成・切り替え・削除、merge、rebase、cherry-pick、tag、remote変更、restore、reset、stash等）とPR・release作成は、明示的な依頼がある場合だけ行う。ユーザーの既存変更を勝手に取り消さない。

## 5. Research, Data, and Secrets

- 設計文書の確定事項を守り、暫定・未決定事項をライブラリの既定値や推測で確定しない。要件や仮定を実装の正当化のために後付けしない。
- model、loss、augmentation、metric、評価条件、split、schema、metadata fieldやデータの意味は、依頼または合意の範囲でのみ変更する。
- taxonomy、欠損値、重複、ラベル、split単位、同一個体・同一グループの関係を推測で補完しない。train・validation・testの分離とリーケージの条件を明示する。
- 研究コードでは、該当する仮説、比較対象、指標、分割、停止条件、seed、device、dtype、checkpoint、config、version、非決定性、計算資源の条件を確認する。探索的分析と主張を支持する実験を区別し、未実行のコードや未確認の出力から研究上の結論を出さない。
- raw data、原本、source of truth、immutableと定義されたファイルは変更しない。構造の確認にはschema、manifest、既存の統計、代表的な小規模サンプルを使い、不必要な全件走査・一括ロードを避ける。
- 生成物は既存の出力先とignore規則を確認して扱い、キャッシュ、モデル、データを不用意にソース管理対象へ追加しない。
- 秘密情報や個人情報を表示・記録・コミットしない。`.env` 等は必要性と安全性を確認した場合だけ読み、ログ・差分・報告にも秘密情報を含めない。

## 6. Implementation and Documents

対象に関係する既存の言語version、設定、命名、責務、公開インターフェース、テストの慣例に従う。無関係なimport整理やlint修正を混ぜず、formatter等の適用範囲を限定する。dependency fileとlock fileは依存関係の変更が合意された場合だけ更新する。

入力は境界で検証し、失敗の原因を特定できる例外・エラーを保つ。既存の抽象化を活用し、不要な抽象化や暗黙のglobal mutable stateを増やさない。I/O・副作用は可能な範囲でロジックから分離する。コメントは制約や判断理由を説明し、入力規模に対して計算量・メモリ・I/Oが問題になる場合は報告する。

Pythonでは次を適用する。

- プロジェクト指定のversionを使い、指定がない新規コードでは3.10以降を前提とする。
- 引数・戻り値に型ヒントを付け、`Any` は可能な限り避ける。複雑なデータ契約にはプロジェクトに合う明示的なschemaを使い、パスには原則 `pathlib.Path` を使う。
- docstringは既存形式に従い、規約がなければNumPyまたはscikit-learn形式とする。
- 具体的な例外型を使い、広すぎる例外捕捉を避ける。import時に重い処理、I/O、環境変更を行わない。

設計文書は、文書変更の依頼または実装依頼に文書同期が明確に含まれる場合に更新する。Markdownの数式はKaTeX互換とし、inlineは `$...$`、displayは前後に空行を置いた `$$...$$` を使う。[KaTeX Supported Functions](https://katex.org/docs/supported)にないコマンドや独自macroを推測で使わず、数式をcode blockで代用しない。

## 7. Validation and Completion

- 依頼と既存仕様から、必要な成果物と完了条件を把握する。調査なら根拠のある回答、変更なら必要な編集と差分確認、実行依頼を含むなら結果確認と必要な修正まで進める。
- テストの追加・変更は依頼、受け入れ条件、既存の開発慣行から必要な範囲に留める。無関係なテスト修正や大規模なテスト基盤の導入は行わない。
- 検証は変更に対応する最小のテストや確認を選ぶ。fixtureやsynthetic dataを優先し、関連テスト・静的検査・統合検証へ広げるのは、変更・失敗・未解決の懸念がある場合に限る。実行には第4節を適用し、十分な根拠が得られた後に同じ検証を繰り返さない。
- 検証コマンドはREADMEや設定から特定し、存在を確認していないコマンドを提示しない。実行が必要で未許可の場合は、編集とread-onlyでできる確認を終え、ユーザーに実行結果の共有を依頼する。
- 完了前に差分を確認し、依頼外の変更やユーザーの変更の取り消しがないことを確かめる。

完了報告では、変更内容、重要な仮定・決定、実施した確認と結果、未実施の検証と理由、必要な実行コマンド・確認点、残る不確実性を、該当するものだけ簡潔に示す。未実行を成功・テスト済みと表現せず、残作業があれば具体的に伝える。
